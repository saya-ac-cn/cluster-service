import Router from './router'
import {BrowserRouter} from 'react-router-dom'
import 'antd/dist/antd.less'

function App() {
  return (
      <BrowserRouter>
          <Router/>
      </BrowserRouter>
  )}
export default App